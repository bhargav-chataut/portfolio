// Fallback generated-content module.
// GitHub Actions overwrites this at build time with fresh pinned repos and Medium posts.

export const liveProjects = [] as const
export const liveWriting = [] as const
